let objectList = ["taula","cadira","ordinador","llibreta"];

for (let index = 0; index < objectList.length; index++) {
    const element = objectList[index];
    console.log("L'objecte " + element + " és a la posició " + index);
}